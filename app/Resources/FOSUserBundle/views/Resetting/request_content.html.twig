{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
    <div class="form-group form-focus">
        <label for="username" class="control-label">{{ 'resetting.request.username'|trans }}</label>
        <input type="text" id="username" name="username" required="required" class="form-control floating"/>
    </div>
    <div class="form-group text-center">
        <input type="submit" value="{{ 'resetting.request.submit'|trans }}" class="btn btn-primary btn-block account-btn"/>
    </div>
    <div class="text-center">
        <a href="{{ path('homepage') }}">Retour à la page d'accueil</a>
    </div>
</form>
